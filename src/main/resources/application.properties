spring.application.name=ai-interview

spring.profiles.active=${SPRING_PROFILES_ACTIVE}

server.forward-headers-strategy=framework

spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

# ================= DATABASE =================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.flyway.enabled=true

# ================= GEMINI =================
gemini.api.key=${GEMINI_API_KEY}
gemini.model=${GEMINI_MODEL}

# ================= STT =================
stt.url=${STT_URL}

# ================= MINIO =================
minio.url=${MINIO_URL}
minio.access-key=${MINIO_ACCESS_KEY}
minio.secret-key=${MINIO_SECRET_KEY}
minio.bucket=${MINIO_BUCKET}
minio.public-url=${MINIO_PUBLIC_URL}

#==============local==============
# gemini.api.key=${GEMINI_API_KEY}
# gemini.model=gemini-2.5-flash

# # ================= DATABASE =================
# # spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
# # spring.datasource.username=postgres
# # spring.datasource.password=FauzyDB

# spring.datasource.url=jdbc:postgresql://34.182.34.97:5432/ai_interview
# spring.datasource.username=dbserver
# spring.datasource.password=Aiinterviewversidua
# # STT Configuration
# stt.url=http://34.83.26.96:8000/media/transcribe

# minio.url: http://localhost:9000
# minio.access-key: minioadmin
# minio.secret-key: minioadmin
# minio.bucket: ai-interview
# minio.public-url: http://localhost:9000